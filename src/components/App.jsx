// import { getImages } from 'services';
import { Searchbar } from './Searchbar';

export const App = () => {
  return (
    <div className="App">
      <Searchbar />
    </div>
  );
};

// const get = async () => {
//   const images = await getImages();
//   console.log(images);
//   return images;
// };
